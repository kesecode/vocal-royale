# Ports (host mappings; reference from compose.yaml if needed)
APP_PORT=3000
BACKEND_PORT=8090

# Initial app admin credentials
ADMIN_EMAIL=admin@vocal.royale
ADMIN_PASSWORD=ChangeMeNow!

# DB credentials
PB_ADMIN_EMAIL=admin_db@vocal.royale
PB_ADMIN_PASSWORD=vocal_royale_2025

# Frontend public URL (set to your deployed domain in prod)
ORIGIN=http://localhost:${APP_PORT}

# Frontend runtime
NODE_ENV=production

# Service-to-service URL inside the compose network (https://www.my-domain.com/)
PB_URL=http://backend:${BACKEND_PORT}

# Log settings
LOG_LEVEL=info
LOG_FORMAT=pretty
LOG_COLOR=true

# Session / auth
# Max cookie age in seconds (default 48h)
SESSION_MAX_AGE=172800

# Apple Music API configuration
SONG_CHOICE_VALIDATE=true
APPLE_MUSIC_KEY_ID=
APPLE_TEAM_ID=
APPLE_MUSIC_STOREFRONT=de
APPLE_MUSIC_TOKEN_TTL_DAYS=7

# Add key directly as a single-line env var with literal \n for newlines.
# Mandatory if SONG_CHOICE_VALIDATE is set to true
# APPLE_MUSIC_PRIVATE_KEY=

# SMTP Configuration for Email Notifications (Mailgun)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@vocal-royale.de

# OpenAI API for Certificate Text Generation
# Certificate emails are only sent when ALL three are configured: OPENAI_API_KEY, CERTIFICATE_PROMPT, and SMTP
OPENAI_API_KEY=

# Certificate prompt template (use \n for newlines)
# Available placeholder: {songsData} (contains all structured data)
CERTIFICATE_PROMPT=Du bist ein charismatischer, leicht sarkastischer Karaoke-Show-Moderator auf einer 30.-Geburtstagsfeier. Schreibe einen kurzen, stimmungsvollen Urkunden-Text in direkter Ansprache (Du-Form).\n\nREGELN:\n- Maximal 500 Zeichen inkl. Leerzeichen.\n- Genau ein Absatz, gut lesbar.\n- Schreibe korrektes Deutsch mit Umlauten (ä, ö, ü, Ä, Ö, Ü) und ß. Keine ae/oe/ue-Umschreibungen.\n- Kein Markdown, keine Listen, keine Emojis.\n- Einstieg NICHT mit Floskeln wie „Herzlichen Glückwunsch“.\n- Sprich den Teilnehmer direkt mit „du“ an.\n- Ton: positiv, locker, humorvoll, trocken-sarkastisch.\n- Je nach Platzierung darfst du liebevoll necken, aber nie beleidigend, verletzend oder fies.\n- Platzierung, Gesamtwertung und Teilnehmerzahl stehen bereits auf der Urkunde: Wiederhole diese Infos NICHT explizit.\n- Nenne Name und Künstlername (verwende die Schreibweise „a.k.a.“).\n- Schreibe erzählerisch und zusammenfassend.\n- Schreibe KEINE Abfolge von Songs und keinen zeitlichen Ablauf.\n- Vermeide Formulierungen wie „bei Song X“, „dann kam“, „und danach“.\n- Songs dürfen genannt werden, aber nur eingebettet als Beispiele oder Referenzen, nicht als Struktur.\n- Nutze maximal EINEN inhaltlichen Detailpunkt aus Kommentaren oder Jurywertungen, wenn er dem Gesamtbild dient.\n- Dialekt oder regionale Färbung ist OPTIONAL und sehr sparsam, nur wenn es natürlich passt.\n- Wenn Kommentare widersprüchlich sind, mach einen charmanten Gag daraus.\n- WICHTIG: Der Text muss vollständig enden (kein Abschneiden, keine „…“).\n- Ausgabe: nur der fertige Text.\n\nPARAMETER:\n{songsData}