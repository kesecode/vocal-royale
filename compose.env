# Ports (host mappings; reference from compose.yaml if needed)
APP_PORT=3000
BACKEND_PORT=8090

# Initial app admin credentials
ADMIN_EMAIL=admin@vocal.royale
ADMIN_PASSWORD=ChangeMeNow!

# DB credentials
PB_ADMIN_EMAIL=admin_db@vocal.royale
PB_ADMIN_PASSWORD=vocal_royale_2025

# Frontend public URL (set to your deployed domain in prod)
ORIGIN=http://localhost:${APP_PORT}

# Frontend runtime
NODE_ENV=production

# Service-to-service URL inside the compose network (https://www.my-domain.com/)
PB_URL=http://backend:${BACKEND_PORT}

# Log settings
LOG_LEVEL=info
LOG_FORMAT=pretty
LOG_COLOR=true

# Session / auth
# Max cookie age in seconds (default 48h)
SESSION_MAX_AGE=172800

# Apple Music API configuration
SONG_CHOICE_VALIDATE=true
APPLE_MUSIC_KEY_ID=
APPLE_TEAM_ID=
APPLE_MUSIC_STOREFRONT=de
APPLE_MUSIC_TOKEN_TTL_DAYS=7

# Add key directly as a single-line env var with literal \n for newlines.
# Mandatory if SONG_CHOICE_VALIDATE is set to true
# APPLE_MUSIC_PRIVATE_KEY=

# SMTP Configuration for Email Notifications (Mailgun)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@vocal-royale.de

# OpenAI API for Certificate Text Generation
# Certificate emails are only sent when ALL three are configured: OPENAI_API_KEY, CERTIFICATE_PROMPT, and SMTP
OPENAI_API_KEY=

# Certificate prompt template (use \n for newlines)
# Available placeholder: {songsData} (contains all structured data)
CERTIFICATE_PROMPT=Du bist ein charismatischer, leicht sarkastischer Karaoke-Show-Moderator auf einer 30.-Geburtstagsfeier. Schreibe einen kurzen Recap-Text fuer eine Urkunden-Mail.\n\nRegeln:\n- Maximal 500 Zeichen inkl. Leerzeichen.\n- Genau ein Absatz, keine Listen, kein Markdown, keine Emojis.\n- Positiv, lustig, trocken-sarkastisch. Je nach Platzierung darfst du liebevoll necken, aber nie beleidigend oder verletzend.\n- Wenn Kommentare + Wertungen eindeutig begeistert sind, feiere die Person sichtbar und reduziere Sarkasmus.\n- Nenne Name, Kuenstlername, Platzierung (und optional von X wenn Teilnehmerzahl vorhanden ist).\n- Erwaehne 1-3 Songs (Titel reichen).\n- Baue 1-2 konkrete, inhaltliche Details aus Kommentaren ein (paraphrasiert, keine Zitate).\n- Nutze die Jurorenwertung (Performance, Gesang, Schwierigkeit) und/oder die Gesamtwertung pro Auftritt fuer einen witzigen, aber verstaendlichen Mini-Callback.\n- Wenn keine Zuschauerkommentare vorhanden sind, erwaehne das nicht.\n- Wenn Bewertungen/Kommentare widerspruechlich sind, mach einen charmanten Gag draus.\n- Ausgabe: nur der fertige Text.\n\nPARAMETER:\n{songsData}
